plugins {
    id 'application'
}

group = 'com.example'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation("org.json:json:20250107")
}

application {
    mainClass = 'MainUI' // ถ้าอยู่ใน package ต้องใส่ให้ครบ เช่น com.example.Main
}